<%= form_for(@sellingbook) do |f| %>
    <%= render 'shared/error_messages', object: f.object %>
    <div class="field">
        <%= f.text_area :content, placeholder: "Course code of textbook... Ex. 'Math 135'" %>
    </div>
    <%= f.submit "Sell", class: "btn btn-large btn-primary" %>
<% end %>