<%= form_for(@wantedbook) do |f| %>
    <%= render 'shared/error_messages', object: f.object %>
    <div class="field">
        <%= f.text_area :content, placeholder: "Course code of textbook... Ex. 'Math 135'" %>
    </div>
    <%= f.submit "Want", class: "btn btn-large btn-primary" %>
<% end %>